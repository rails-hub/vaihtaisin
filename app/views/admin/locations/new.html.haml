%h1 New location

= render 'form'

= link_to 'Back', locations_path


:javascript
  $("#location_city").on({
      focus: function(){
      },
      blur: function() {
        leaflet_map(null, null);
      }
  });
